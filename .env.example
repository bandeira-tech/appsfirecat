# Apps Firecat Configuration

# B3nd Backend
BACKEND_URL=https://testnet-evergreen.fire.cat

# Site Identity (for deploying the marketing site)
# Generate new keys with: deno run -A scripts/deploy-site.ts
SITE_PUBKEY=
SITE_PRIVATE_KEY=

# Host Target (where to serve content from)
# Can be:
# - Direct path: immutable://accounts/{pubkey}/site/
# - Mutable pointer: mutable://accounts/{pubkey}/target
TARGET=

# Server Port
PORT=8080

# Host Identity (optional, generated if not provided in dev mode)
HOST_PUBKEY=
HOST_PRIVATE_KEY=
